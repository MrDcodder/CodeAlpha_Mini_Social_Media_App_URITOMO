# Implementation Plan

- [x] 1. Add `getAvatarUrl()` helper with initials fallback to `app.js`
- [x] 2. Restructure `seed.js` - 1 admin + 5 users, remove post seeding
- [x] 3. Fix `messages.html` - add dark mode CSS, fix body class, fix broken `<img>` tag
- [x] 4. Fix `search.html` - add dark mode CSS, add autocomplete suggestions dropdown
- [x] 5. Update avatar rendering across all HTML files to use fallback
- [x] 6. Delete old database so seed can recreate
